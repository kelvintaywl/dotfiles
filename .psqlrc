-- set default display of null values
\pset null '(null)'

-- separate history log files by DB names
\set HISTFILE ~/.psql_history- :DBNAME

-- autocomplete keywords to uppercases
\set COMP_KEYWORD_CASE upper

-- nicely formatted tables FTW
\x auto

-- we want verbose error reports
\set VERBOSITY verbose
